# ao-small repro

读取 `small-scope.json` 和用户 goal。建立可观察信号。

必须写入：

- `$AO_ARTIFACTS_DIR/repro.md`

`repro.md` 必须包含 symptom、expected behavior、actual behavior、reproduction or signal、suspected scope、regression test 和 verification。
