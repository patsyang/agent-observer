# Agent Observer 0.1 MVP 设计 - Tasks

## 状态

READY

## 拆分原则

- 按垂直切片拆分，不按“先全部后端、再全部前端”的横向层拆分。
- 每个任务应能在一次聚焦会话内完成、测试、验证。
- 任务规模只能是 XS、S、M、L；出现 XL 必须继续拆分。
- 每个任务必须有验收、验证命令、依赖和可能影响文件。

## 任务列表

### Task 1: <短标题>

- **状态**：READY
- **目标**：说明该任务完成后用户或系统能观察到什么。
- **垂直切片**：说明是否覆盖后端、前端、collector、数据库、测试；没有覆盖的说明原因。
- **可能影响文件**：
  - `path/to/file`
- **实现要点**：
  - 复用现有模式，不引入重复抽象。
- **验收**：
  - [ ] 可通过命令、文件、接口响应或 UI 状态判断。
- **验证**：`python scripts/ao.py verify`
- **依赖**：无
- **规模**：M
- **风险/GOTCHA**：无

## 检查点

### Checkpoint: 首个垂直切片

- [ ] 受影响测试通过。
- [ ] 没有扩大 Spec 范围。
- [ ] 文件规模仍符合 AGENTS.md。
